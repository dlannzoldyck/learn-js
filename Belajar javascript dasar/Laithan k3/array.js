// array
function myFunc(){
    return
}
let array = ['string', true, 8, myFunc,[3,2,1]];
// console.log(array[4][2])

// manipulasi array

var arry = [];
/*
    1. Menambah array

    arry[1] = "boby"

    2. Menghapus array 

    arry[1] = undefined;
    console.log(arry);

    3. Menampilkan isi array 

    arry = [1,2,3];

    for( let i = 0; i < arry.length; i++){
        console.log(`Mahasiswa ke- ${i+1} : ${arry[i]}`);
    }
*/

/*
    array method
    var arry = ['sandika','galih','whayu'];
    1. Join
    console.log(arr.join(' - '));
    output = sandika - galih - whayu;
    Mengabungkan array
    2. push, pop
    arry.push('ahmad'); // sandika, galih, whayu, ahmad
    arry.pop(); // sandika, galih
    3. unshift dan shift
    arry.unshift('fadlan'); // sama kek push
    arry.shift(); // sama dengan pop
    4. splice
    console.log(arr.join(' - '));


*/